version: "3"
services:
  web:
    image: nginx:alpine
    container_name: dappswarm-demo
    ports:
      - "8081:80"
    volumes:
      - ./static:/usr/share/nginx/html:ro
    restart: unless-stopped
