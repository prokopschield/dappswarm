version: "3"
services:
  web:
    image: nginx:alpine
    container_name: dappswarm-hello
    ports:
      - "8080:80"
    volumes:
      - ./index.html:/usr/share/nginx/html/index.html:ro
    restart: unless-stopped
